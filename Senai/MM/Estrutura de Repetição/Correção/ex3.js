// 3. Faça um programa que imprima os números ímpares de 0 a 50;

for (let contador = 0; contador <= 50; contador++){
    // Se a conta der diferente de 0 entra no if
    if (contador % 2 !== 0 ) {
        console.log(contador);
    }
}