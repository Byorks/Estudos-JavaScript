// Vai apresentar na tela os números repetidas vezes


// contador é o iterador, variável contadora | condicao para acontecer a condicao | contador soma mais 1
for (let contador = 1; contador <= 10; contador++) {
    console.log(contador);
}